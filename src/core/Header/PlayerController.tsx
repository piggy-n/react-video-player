import * as React from 'react';
import type { FC } from 'react';
import type { PlayerControllerProps } from '@/core/Header/type';

const PlayerController: FC<PlayerControllerProps> = () => {
    return (
        <div/>
    );
};

export default PlayerController;