export type Position = {
    x: number;
    y: number;
}

export type Size = {
    width: number;
    height: number;
}

export type Feature = {
    stream: boolean,
    control: boolean,
    record: boolean,
}
