import Icon from '@/components/Icon';
import * as React from 'react';

const Close = () => {
    return (
        <Icon
            name={'close'}
            title={'关闭'}
        />
    );
};

export default Close;
